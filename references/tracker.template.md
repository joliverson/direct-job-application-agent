# Tracker template

Copy this to private/tracker.md if you use an application tracker.

- Tracker URL or location:
- Application tab and header row:
- Columns for date, company, title, direct employer URL, source, document versions, status, salary, next action, notes, and confirmation:
- Allowed dropdown values:
- Duplicate detection key (for example, company plus requisition ID):

Read live metadata and target cells before edits. Record only verified submissions as applied. Keep passwords and other credentials out of the tracker.
